info:
  name: All User
  type: http
  seq: 25

http:
  method: GET
  url: http://localhost:5000/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
