info:
  name: Get Analytics Charts
  type: http
  seq: 38

http:
  method: GET
  url: http://localhost:5000/api/analytics/charts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
