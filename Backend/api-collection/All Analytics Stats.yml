info:
  name: All Analytics Stats
  type: http
  seq: 36

http:
  method: GET
  url: http://localhost:5000/api/analytics/stats
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
