info:
  name: Delete User
  type: http
  seq: 27

http:
  method: DELETE
  url: "{{base_url}}/users/69bcede0c13124293000e209"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:5000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
