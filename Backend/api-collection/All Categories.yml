info:
  name: All Categories
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/categories"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://localhost:5000/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
